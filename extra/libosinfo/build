#!/bin/sh -uxue
export DESTDIR="$1"

patch -Np1 -i use_soup3.patch

meson build \
    ${MESON_OPTIONS} \
    -D with-usb-ids-path=/usr/share/hwdata/usb.ids \
    -D with-pci-ids-path=/usr/share/hwdata/pci.ids

ninja -C build
ninja -C build install
