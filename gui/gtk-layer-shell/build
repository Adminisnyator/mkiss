#!/bin/bash -uxe
export DESTDIR="$1"

meson setup build \
    --prefix=/usr \
    --wrap-mode=nofallback \
    --buildtype=plain \
    -D tests=false \
    -D docs=false 

ninja -C build
ninja -C build install
